<UserControl x:Class="UrbanPlanToolbox.Controls.EditableTagList" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel Spacing="6">
    <TextBlock x:Name="LabelTextBlock" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap" />
    <ItemsControl x:Name="ItemsView">
      <ItemsControl.ItemsPanel><ItemsPanelTemplate><ItemsWrapGrid Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate><DataTemplate>
        <Border Padding="8,4" Margin="0,0,6,6" CornerRadius="12" Background="{ThemeResource CardStrokeColorDefaultBrush}">
          <StackPanel Orientation="Horizontal" Spacing="4"><TextBlock Text="{Binding}" TextWrapping="Wrap"/><Button Content="×" Tag="{Binding}" Click="OnRemoveClick" Padding="4,0" AutomationProperties.Name="Remove tag" /></StackPanel>
        </Border>
      </DataTemplate></ItemsControl.ItemTemplate>
    </ItemsControl>
    <Grid ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox x:Name="InputBox" Grid.Column="0" KeyDown="OnInputKeyDown" TextWrapping="Wrap"/><Button x:Name="AddButton" Grid.Column="1" Click="OnAddClick" AutomationProperties.Name="Add item" /></Grid>
  </StackPanel>
</UserControl>
