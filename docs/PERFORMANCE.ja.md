# パフォーマンス管理

v1.7.0 では、パッケージ サイズ、依存関係 inventory、native allowlist という決定的な管理項目だけを検証します。起動時間、idle memory、代表ツールの読み込み、その他の実 WinUI ウィンドウ制御を必要とする項目は、明示的に対象外でありリリース条件ではありません。OpenCvSharp などの重い依存関係は機能の使用時だけ初期化し、この方針は UI 駆動の計測ではなくソースレベルの依存関係管理で検証します。
