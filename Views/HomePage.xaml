<Page x:Class="UrbanPlanToolbox.Views.HomePage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ScrollViewer HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
    <StackPanel Style="{StaticResource PageContentStackPanelStyle}">
      <Grid ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <StackPanel><TextBlock x:Uid="ProjectHome_Title" Style="{StaticResource TitleTextBlockStyle}"/><TextBlock x:Uid="ProjectHome_Description" TextWrapping="Wrap"/></StackPanel>
        <Button x:Uid="ProjectHome_NewProject" Grid.Column="1" Click="OnNewProject" VerticalAlignment="Top"/>
      </Grid>
      <ListView x:Name="KindList" IsItemClickEnabled="True" SelectionMode="Single" SelectionChanged="OnKindChanged"
                ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollMode="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollMode="Disabled">
        <ListView.ItemsPanel><ItemsPanelTemplate><ItemsStackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListView.ItemsPanel>
        <ListView.ItemTemplate><DataTemplate><TextBlock Text="{Binding DisplayName}" Margin="8,2"/></DataTemplate></ListView.ItemTemplate>
      </ListView>
      <InfoBar x:Name="StatusBar" IsOpen="False"/>
      <StackPanel x:Name="EmptyPanel" Spacing="10" Visibility="Collapsed">
        <TextBlock x:Name="EmptyText" TextWrapping="Wrap"/>
        <Button x:Name="EmptyCreateButton" Click="OnCreateCurrentKind" HorizontalAlignment="Left"/>
      </StackPanel>
      <ItemsControl x:Name="ProjectsList">
        <ItemsControl.ItemTemplate><DataTemplate><Button Tag="{Binding Project}" Click="OnOpenProject" Style="{StaticResource CardActionButtonStyle}" Margin="0,0,0,10">
          <Grid ColumnSpacing="16"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Spacing="4"><StackPanel Orientation="Horizontal" Spacing="8"><TextBlock Text="{Binding Name}" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap"/><Border Padding="8,2" CornerRadius="10" Background="{ThemeResource CardStrokeColorDefaultBrush}"><TextBlock Text="{Binding Kind}"/></Border></StackPanel><TextBlock Text="{Binding PrimaryDetails}" TextWrapping="Wrap"/><TextBlock Text="{Binding SecondaryDetails}" TextWrapping="Wrap" ToolTipService.ToolTip="{Binding FullDetails}"/><TextBlock Text="{Binding Statistics}"/></StackPanel>
            <TextBlock Grid.Column="1" Text="{Binding Updated}" VerticalAlignment="Center" TextWrapping="Wrap"/>
          </Grid>
        </Button></DataTemplate></ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </ScrollViewer>
</Page>
